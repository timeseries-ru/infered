Define unknown, which we want to know
mean ~ GaussianFromMeanAndVariance(0.0, 0.1) / 2.0

Define the coefficient
alpha ~ -GaussianFromMeanAndVariance(mean, 0.01)

Test some ops
beta ~ (1.0 + alpha) * X1

Some residuals?
eps ~ GaussianFromMeanAndVariance(0.0, 0.1)

Define target
Y ~ Exp(X1 + beta + eps)
